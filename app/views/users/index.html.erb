<div class="container">
  <h2>ユーザー</h2>
  <% @users.each do |user| %>
      <div class="container-child col-md-2 col-sm-4 col-xs-6 ">
        <%= profile_img(user) %>
        <p><%= link_to '詳細ページ', user_path(user.id) %></p>
        <p><%= user.name %></p>
        <p><%= user.email %></p>
        <%= render 'follow_form', user: user %>
      </div>
  <% end %>
</div>
